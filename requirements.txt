fastapi>=0.110
uvicorn[standard]>=0.27
chromadb>=1.0
pydantic>=2.0
scikit-learn>=1.3
numpy>=1.24
# Recommended for best Vietnamese retrieval (downloads model on first run):
sentence-transformers>=2.6
# Optional: only needed if you set ANTHROPIC_API_KEY for Claude-generated answers
anthropic>=0.40
